

-- post 0.1

- validate call should be optional - most common use case is non-async - need to be able to throw an exception

- ctxt.util.proplist not good name - actually gets single or list of anything

- test/document validate return value if no async calls
- tests for type child rules
- test for only$ rule
- how to support required$: true
- default$ rule
- function$ rule - applies function as rule
- deeper self-vaidation
- list$, prop$ validation, error handling
- http request example - agent - type should accept array (OR semantics)
- check for truth failures when gex returns a 0 or '' - wild$ rule
- extend docs for custom rules




